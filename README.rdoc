== README

Simple cookbook. Create, add images, update and delete recipes.

`git clone` this repo
* `mkdir my_cookbook`
* change directory `cd my_cookbook`
* load the schema with `rake db:schema:load`
* run rails server with `rails s`
* navigate to localhost:3000/recipes
earch engines, etc.)
